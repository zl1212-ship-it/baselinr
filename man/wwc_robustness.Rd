% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robustness.R
\name{wwc_robustness}
\alias{wwc_robustness}
\title{Robustness of the WWC baseline-equivalence verdict}
\usage{
wwc_robustness(data, treatment, covariates = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{treatment}{String naming the treatment-indicator column (see
\code{\link[=baseline_equivalence]{baseline_equivalence()}}).}

\item{covariates}{Character vector of covariate columns. Defaults to all
eligible columns other than \code{treatment}.}
}
\value{
A data frame with one row per covariate and the columns \code{covariate},
\code{type}, \code{category_default} (the category under baselinr's default), the set
of \code{categories} the covariate takes across the defensible choices, \code{flips}
(whether that set has more than one category), and \code{abs_es_min} /
\code{abs_es_max} (the range of the absolute effect size across choices). The
overall verdict under each choice is attached as \code{attr(x, "overall")}, and
\code{attr(x, "overall_stable")} is \code{TRUE} when the overall verdict is invariant.
}
\description{
Reports how stable a baseline-equivalence verdict is to the computation
choices a careful analyst might defensibly make differently. For each
continuous covariate it recomputes the standardized difference under the
cross of two choices, standardizing by the pooled versus the comparison-group
standard deviation and applying the What Works Clearinghouse (WWC)
small-sample correction or not, and records whether the covariate's WWC
category changes. Binary covariates use the Cox index, which does not depend
on these choices. It also reports whether the overall verdict changes.
}
\details{
This is a multiverse, or specification-curve, view of a single WWC
determination: it shows whether the verdict depends on which defensible
choice is made.
}
\examples{
df <- data.frame(
  treat = c(1, 1, 1, 0, 0, 0),
  pretest = c(5, 6, 7, 4, 5, 6),
  female = c(1, 0, 1, 0, 0, 1)
)
r <- wwc_robustness(df, treatment = "treat")
r
attr(r, "overall")

}
\references{
What Works Clearinghouse (2022). \emph{Procedures Handbook}
(Version 5.0). U.S. Department of Education. Steegen, S., Tuerlinckx, F.,
Gelman, A., & Vanpaemel, W. (2016). Increasing transparency through a
multiverse analysis. \emph{Perspectives on Psychological Science}, 11(5), 702-712.
}
