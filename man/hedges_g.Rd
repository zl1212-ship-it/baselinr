% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baselinr.R
\name{hedges_g}
\alias{hedges_g}
\title{Hedges' g standardized mean difference}
\usage{
hedges_g(x, treatment, na.rm = TRUE)
}
\arguments{
\item{x}{Numeric vector of covariate values.}

\item{treatment}{Vector the same length as \code{x} identifying group
membership. Must have exactly two unique non-missing values. The larger
value (e.g. \code{1}, \code{TRUE}, or the second sorted level) is treated as
the treatment group; the other as the comparison group.}

\item{na.rm}{Logical; drop rows where \code{x} or \code{treatment} is \code{NA}.
Default \code{TRUE}.}
}
\value{
A single numeric value: Hedges' g. Positive when the treatment
group mean exceeds the comparison group mean.
}
\description{
Computes the standardized mean difference (Hedges' g) between a treatment
and a comparison group for a single numeric covariate, using the pooled
within-group standard deviation and the small-sample correction factor used
by the What Works Clearinghouse (WWC).
}
\details{
The correction factor is \eqn{\omega = 1 - 3 / (4N - 9)}, where
\eqn{N = n_{treatment} + n_{comparison}}.
}
\examples{
x <- c(5, 6, 7, 4, 5, 6)
g <- c(1, 1, 1, 0, 0, 0)
hedges_g(x, g) # 0.8

}
\references{
What Works Clearinghouse (2022).
\emph{Procedures Handbook} (Version 5.0). U.S. Department of Education.
}
