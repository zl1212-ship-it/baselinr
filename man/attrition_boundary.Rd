% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attrition_boundary.R
\name{attrition_boundary}
\alias{attrition_boundary}
\title{Classify a study under the WWC attrition standard}
\usage{
attrition_boundary(
  overall,
  differential,
  assumption = c("cautious", "optimistic")
)
}
\arguments{
\item{overall}{Overall attrition, as a proportion in \verb{[0, 1]} (as returned by
\code{\link[=attrition]{attrition()}}).}

\item{differential}{Differential attrition, as a proportion (the absolute
difference in group attrition rates, as returned by \code{\link[=attrition]{attrition()}}).}

\item{assumption}{Which boundary to apply: \code{"cautious"} (default) or
\code{"optimistic"}.}
}
\value{
A data frame, one row per input, with columns \code{overall},
\code{differential}, \code{assumption}, \code{max_differential} (the highest differential
attrition still counted as low, as a proportion; \code{NA} where the overall rate
is beyond the boundary), and \code{attrition} (\code{"low"} or \code{"high"}).
}
\description{
Given a study's overall and differential attrition, classifies it as low or
high attrition against the What Works Clearinghouse (WWC) attrition boundary
(Standards Handbook Version 4.1, Table II.1). This is the classification that
\code{\link[=attrition]{attrition()}} deliberately leaves to the user: \code{\link[=attrition]{attrition()}} reports the
rates, \code{attrition_boundary()} applies the standard.
}
\details{
The WWC uses one of two boundaries. The \strong{cautious} boundary is applied when
the intervention could plausibly affect attrition (for example, a dropout
prevention program); the \strong{optimistic} boundary when it is unlikely to (for
example, a first-grade reading program). The applicable boundary is set by the
review protocol, not chosen post hoc; the default here is the more
conservative cautious boundary.
}
\examples{
# A study with 8\% overall and 3 percentage-point differential attrition:
attrition_boundary(0.08, 0.03) # low under the cautious boundary

# 30\% overall, 6-point differential: high if cautious, low if optimistic
attrition_boundary(0.30, 0.06, "cautious")
attrition_boundary(0.30, 0.06, "optimistic")

# Chained from attrition():
set.seed(1)
g <- rep(c(1, 0), each = 100)
kept <- rbinom(200, 1, ifelse(g == 1, 0.9, 0.82))
a <- attrition(g, kept)
attrition_boundary(a$attrition_overall, a$differential_attrition)

}
\references{
What Works Clearinghouse (2020). \emph{Standards Handbook, Version
4.1}, Table II.1. U.S. Department of Education.
}
