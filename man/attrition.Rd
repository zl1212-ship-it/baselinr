% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{attrition}
\alias{attrition}
\title{Overall and differential attrition}
\usage{
attrition(treatment, retained, na.rm = TRUE)
}
\arguments{
\item{treatment}{Vector identifying group membership; exactly two unique
non-missing values (the larger is treated as the treatment group, as in
\code{\link[=hedges_g]{hedges_g()}}).}

\item{retained}{Logical (or \code{0/1}) the same length as \code{treatment}: \code{TRUE}/\code{1}
for cases retained in the analytic sample, \code{FALSE}/\code{0} for those lost.}

\item{na.rm}{Logical; drop rows where \code{treatment} or \code{retained} is \code{NA}.
Default \code{TRUE}.}
}
\value{
A one-row data frame with columns \code{attrition_overall},
\code{attrition_treatment}, \code{attrition_comparison}, and
\code{differential_attrition} (all proportions).
}
\description{
Computes overall and differential sample attrition for a two-group design,
the inputs to the What Works Clearinghouse (WWC) attrition standard.
Differential attrition is the absolute difference between the treatment and
comparison attrition rates.
}
\details{
This function reports the attrition rates; it does not classify them. Compare
the overall and differential rates against the WWC attrition boundary for
your chosen response assumption (cautious or optimistic) in the Procedures
Handbook.
}
\examples{
set.seed(1)
g <- rep(c(1, 0), each = 100)
kept <- rbinom(200, 1, ifelse(g == 1, 0.9, 0.8))
attrition(g, kept)

}
\references{
What Works Clearinghouse (2022).
\emph{Procedures Handbook} (Version 5.0). U.S. Department of Education.
}
