% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{love_plot}
\alias{love_plot}
\title{Love plot of standardized effect sizes}
\usage{
love_plot(equivalence, signed = FALSE)
}
\arguments{
\item{equivalence}{A data frame returned by \code{\link[=baseline_equivalence]{baseline_equivalence()}}.}

\item{signed}{Logical. If \code{FALSE} (default), plot absolute effect sizes with
reference lines at 0.05 and 0.25. If \code{TRUE}, plot signed effect sizes with
symmetric reference lines and a line at zero.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the standardized effect size for each covariate from
\code{\link[=baseline_equivalence]{baseline_equivalence()}}, with reference lines at the What Works
Clearinghouse (WWC) thresholds (0.05 and 0.25) and points coloured by WWC
category. Requires the \pkg{ggplot2} package.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  df <- data.frame(
    treat = c(1, 1, 1, 0, 0, 0),
    pretest = c(5, 6, 7, 4, 5, 6),
    female = c(1, 0, 1, 0, 0, 1)
  )
  love_plot(baseline_equivalence(df, "treat"))
}

}
