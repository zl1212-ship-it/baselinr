% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baselinr.R
\name{cox_index}
\alias{cox_index}
\title{Cox index for a binary covariate}
\usage{
cox_index(x, treatment, na.rm = TRUE)
}
\arguments{
\item{x}{A binary covariate (numeric \code{0/1}, logical, two-level factor, or any
vector with exactly two unique non-missing values). The larger value (e.g.
\code{1}, \code{TRUE}, or the second sorted level) is treated as the "event".}

\item{treatment}{Vector the same length as \code{x} identifying group membership;
exactly two unique non-missing values (see \code{\link[=hedges_g]{hedges_g()}}).}

\item{na.rm}{Logical; drop rows where \code{x} or \code{treatment} is \code{NA}.
Default \code{TRUE}.}
}
\value{
A single numeric value: the Cox index. Returns \code{NA} (with a warning)
when a group proportion is exactly 0 or 1, where the index is undefined.
}
\description{
Computes the What Works Clearinghouse (WWC) Cox index, a standardized effect
size for a binary (dichotomous) covariate. The Cox index places the
difference between two proportions on a scale comparable to Hedges' g, so it
can be classified with the same baseline-equivalence thresholds.
}
\details{
The index is \eqn{d_{Cox} = (\mathrm{logit}(p_t) - \mathrm{logit}(p_c)) /
1.65}, where \eqn{p_t} and \eqn{p_c} are the proportions in the "event"
category for the treatment and comparison groups.
}
\examples{
x <- c(1, 1, 1, 1, 0, 1, 0, 0)
g <- c(1, 1, 1, 1, 0, 0, 0, 0)
cox_index(x, g)

}
\references{
What Works Clearinghouse (2022).
\emph{Procedures Handbook} (Version 5.0). U.S. Department of Education.
}
